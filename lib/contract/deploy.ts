export const deploy = async (contract: any, args: any) => {
    let contractAddress = '0x123'

    // const contractAddress = await contract.deploy(args)
    // console.log('contractAddress', contractAddress)
    return contractAddress
}
